{
  "name": "smartly-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Marketing, documentation and brand site for the smartly CLI.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node scripts/check-links.mjs",
    "build:subpath": "BASE_PATH=/smartly-cli astro build && BASE_PATH=/smartly-cli node scripts/check-links.mjs",
    "build:only": "astro build",
    "build:assets": "node scripts/build-assets.mjs",
    "check:links": "node scripts/check-links.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.0",
    "github-slugger": "^2.0.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
